/**
 * expo-iap 5.6.0 ErrorCode.UserCancelled is "user-cancelled".
 * Kept in a tiny module so processor tests do not import the native package.
 */
export const EXPO_IAP_USER_CANCELLED = "user-cancelled";
