/**
 * Compact keyboard-safe dropdown — opens a modal list so options never sit
 * behind the keyboard or overlap sibling fields.
 */
export { SelectField as SmartDropdown, type SelectOption, type SelectFieldProps as SmartDropdownProps } from "@/components/ui/SelectField";
