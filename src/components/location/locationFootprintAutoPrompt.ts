/**
 * V1: tab-mounted LocationFootprintConsentHost must not auto-open after
 * Workspace Ready → You. Re-enable only in a later bounded contextual phase.
 */
export const LOCATION_FOOTPRINT_AUTO_PROMPT_ENABLED_V1 = false;
