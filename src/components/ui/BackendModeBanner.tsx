import React from "react";

/**
 * Dev/backend mode banners are intentionally hidden in UI.
 * Boot intro splash (`VyaamikkIntroSplash`) remains unchanged.
 */
export function BackendModeBanner() {
  return null;
}
