# Vyaamikk onboarding UX harness

Isolated **Expo Go** presentation host. Not authentication.

```bash
npm run start:onboarding-ux-harness
```

Opens on port **8091**. Scan with Expo Go (`host.exp.exponent`).

Does **not** use or replace `com.specialsoftwares.vyaamikkdiary`.
