import { StatutoryInformationScreen } from "@/screens/StatutoryInformationScreen";

/** Statutory list — opened from Settings & Info (not a bottom tab). */
export default function StatutoryInformationRoute() {
  return <StatutoryInformationScreen />;
}
